//task 2
//container tag is tag which can have children, unlike self-closing tag

//task 3
//children is a special prop that allows a component to display the content
// placed inside it. It is part of the props (props.children) and is used to
// dynamically pass inner content using the <Component>...</Component> structure.